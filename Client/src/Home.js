import React from "react";
import { Header, Grid } from "semantic-ui-react";
import './style.css';

export default () => (
  <Grid centered columns={1} id="home">
    <Header as="h1">Home Screen</Header>
  </Grid>
);
