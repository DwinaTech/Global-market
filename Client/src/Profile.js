import React from "react";
import { Header, Grid } from "semantic-ui-react";
import './style.css';

export default () => (
  <Grid centered columns={1} id="login">
    <Header as="h1">Profile Screen</Header>
  </Grid>
);
