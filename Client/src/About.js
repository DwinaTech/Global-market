import React from "react";
import { Header, Grid } from "semantic-ui-react";
import './style.css';

export default () => (
  <Grid centered columns={1} id="about">
    <Header as="h1">About Screen</Header>
  </Grid>
);
